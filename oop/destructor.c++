#include<iostream>
//DESTRUCTOR= #Fuction is called when object is deleted
//            #cannot pass any parameter 
//            #name -- ~(class.name)
using namespace std;
// now we use this in privious one;